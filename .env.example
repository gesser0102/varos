# Database URLs (Get these from your Supabase project settings)
#
# Como obter as URLs:
# 1. Acesse seu projeto no Supabase (https://supabase.com/dashboard)
# 2. Vá em Settings → Database
# 3. Role até "Connection string"
# 4. Copie a "Connection pooling" URL (Transaction mode) para DATABASE_URL
# 5. Copie a "Direct connection" URL para DIRECT_URL
# 6. Substitua [YOUR-PASSWORD] pela senha do seu banco
#
# DATABASE_URL: Usa PgBouncer para connection pooling (recomendado para produção)
# - pgbouncer=true: Usa o pool de conexões do Supabase
# - connection_limit=20: Máximo de 20 conexões simultâneas
# - pool_timeout=30: Timeout de 30 segundos para obter conexão do pool
#
# DIRECT_URL: Conexão direta (necessária para Prisma migrations)

DATABASE_URL="postgresql://postgres:senha@db.xxx.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=10"
DIRECT_URL="postgresql://postgres:senha@db.xxx.pooler.supabase.com:6543/postgres"