# Database URLs (Get these from your Supabase project settings)
# DATABASE_URL usa PgBouncer para connection pooling (melhor para produção)
# Configurações otimizadas para performance:
# - pgbouncer=true: Usa o pool de conexões do Supabase
# - connection_limit=20: Máximo de 20 conexões simultâneas (aumentado)
# - pool_timeout=30: Timeout de 30 segundos para obter conexão do pool (aumentado)
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres?pgbouncer=true&connection_limit=20&pool_timeout=30"
DIRECT_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres"
